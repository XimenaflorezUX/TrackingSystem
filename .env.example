# Copie a `.env.local` si necesita sobreescribir variables en desarrollo.
# En local, deje vacío: Vite reenvía `/vacancies` y `/health` al API (puerto 4000).

# Producción: URL pública del API (sin barra final).
# VITE_API_BASE_URL=https://tu-api.onrender.com
